define(['jquery', 'bootstrap', 'backend', 'table', 'form'], function ($, undefined, Backend, Table, Form) {

    var Controller = {
        index: function () {
            // 初始化表格参数配置
            Table.api.init({
                extend: {
                    index_url: 'tsinghua_member/index' + location.search,
                    add_url: 'tsinghua_member/add',
                    edit_url: 'tsinghua_member/edit',
                    del_url: 'tsinghua_member/del',
                    multi_url: 'tsinghua_member/multi',
                    table: 'tsinghua_member',
                }
            });

            var table = $("#table");

            // 初始化表格
            table.bootstrapTable({
                url: $.fn.bootstrapTable.defaults.extend.index_url,
                pk: 'id',
                sortName: 'id',
                columns: [
                    [
                        {checkbox: true},
                        {field: 'id', title: __('Id')},
                        {field: 'name', title: __('Name')},
                        {field: 'head_img', title: __('Head_img')},
                        {field: 'type', title: __('Type'), searchList: {"Professor":__('Type professor'),"Postdoctorate":__('Type postdoctorate'),"Graduate Students":__('Type graduate students'),"Undergraduate Students":__('Type undergraduate students')}, formatter: Table.api.formatter.normal},
                        {field: 'operate', title: __('Operate'), table: table, events: Table.api.events.operate, formatter: Table.api.formatter.operate}
                    ]
                ]
            });

            // 为表格绑定事件
            Table.api.bindevent(table);
        },
        add: function () {
            Controller.api.bindevent();
        },
        edit: function () {
            Controller.api.bindevent();
        },
        api: {
            bindevent: function () {
                Form.api.bindevent($("form[role=form]"));
            }
        }
    };
    return Controller;
});